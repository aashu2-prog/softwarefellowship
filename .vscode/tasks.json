{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Build Raylib Project",
      "type": "shell",
      "command": "mkdir -p bin && g++ src/main.cpp -o bin/main -I/opt/homebrew/include -L/opt/homebrew/lib -lraylib -framework OpenGL -framework Cocoa -framework IOKit",
      "group": "build",
      "problemMatcher": [],
      "detail": "Compiles Raylib project and creates bin directory if missing"
    }
  ]
}
